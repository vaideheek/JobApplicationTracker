# JobTrack Frontend Environment Variables

# The backend base URL (no trailing slash). E.g. https://jobtrack-api.onrender.com
# Leave empty in local development to fallback to the Vite local dev proxy.
VITE_API_URL=https://jobtrack-api.onrender.com
